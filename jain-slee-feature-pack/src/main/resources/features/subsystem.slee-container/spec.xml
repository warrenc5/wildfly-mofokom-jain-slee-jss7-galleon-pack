<?xml version="1.0" ?>

<feature-spec xmlns="urn:jboss:galleon:feature-spec:1.0" name="subsystem.slee-container">
    <provides>
        <capability name="$profile.org.telestax.slee.container.lib"/>
    </provides>

    <provides>
        <capability name="$profile.org.telestax.slee.container.extension"/>
    </provides>

    <annotation name="feature-branch">
        <elem name="id" value="subsystem.slee-container"/>
    </annotation>
    <!--
<requires>
    <capability name="$profile.org.wildfly.ejb3.remote.client-configurator"/>
    <capability name="$profile.org.wildfly.ejb3"/>
    <capability name="$profile.org.wildfly.ejb3.clustered.singleton"/>
    <capability name="$profile.org.wildfly.transactions.global-default-local-provider"/>
</requires>
    -->

    <refs>
        <ref feature="extension" include="true"/>
        <ref feature="profile" nillable="true"/>
    </refs>

    <params>
        <param name="extension" default="org.telestax.slee.container.extension"/>
        <param name="profile" feature-id="true" default="GLN_UNDEFINED"/>
        <param name="host" default="GLN_UNDEFINED"/>
    </params>

    <packages>
        <package name="org.telestax.slee.container.extension"/>
    </packages>
</feature-spec>